# Pasta2 - Analytics
## Files

```
pasta2_analytics/
├── sql/
│   ├── daily_hourly_avg_passenger_count.sql   # SQL query to get the average passenger count for each day and hour
│   ├── yearly_monthly_avg_total_amount.sql    # SQL query to get the average total amount of a trip per month
├── src/
│   ├── main.py     # Python script to run this pipeline. Runs the queries and saves the results in the output bucket
```